import { CaseStatusPipe } from './case-status.pipe';

describe('CaseStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new CaseStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
